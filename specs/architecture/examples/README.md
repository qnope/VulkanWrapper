# Examples

`examples/` directory

Two example applications demonstrating VulkanWrapper usage:

- [Application](Application/README.md) — Basic windowed Vulkan application setup
- [Advanced](Advanced/README.md) — Full deferred rendering pipeline with ray tracing

Both examples use the `App` class from Application as the base setup (instance, device, allocator, window, swapchain).
